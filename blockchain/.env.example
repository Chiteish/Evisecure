# Blockchain node port (default: 6000)
BLOCKCHAIN_PORT=6000

# Shared secret — backend must send this as x-api-key header
BLOCKCHAIN_API_KEY=evisecure-blockchain-secret-key

# Proof-of-Authority block signing secret
AUTHORITY_SECRET=evisecure-authority-key-2024
